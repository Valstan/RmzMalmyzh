# AGENTS.md — entry point для AI-сессий «RmzMalmyzh»

Канонические проектные правила для любого AI-агента. Перед любым действием прочитай
этот файл полностью: здесь зафиксированы контекст, границы и порядок работы.

Проект — новый сайт АО «Малмыжский завод по ремонту дизельных двигателей» (замена rmz43.ru на WordPress). Проект экосистемы brain_matrica. Kickoff-план: [`../brain_matrica/docs/plans/rmz-site-kickoff.md`](../brain_matrica/docs/plans/rmz-site-kickoff.md), карточка: [`../brain_matrica/projects/RmzMalmyzh.md`](../brain_matrica/projects/RmzMalmyzh.md).

## Быстрые факты

- **Прод:** https://рмз.вмалмыже.рф/ — Бокс Сабантуя, nginx, TLS есть. Стадия 2: Next standalone + Payload, **третий Node-жилец** (:3002, systemd `rmz`, MemoryMax=512M) рядом с Sabantuy (:3000) и Kazanskaya (:3001).
- **Стек:** Next.js 15 App Router + TS + Tailwind 4 + **Payload 3.75 + PostgreSQL** (стандарт экосистемы, образец — KazanskayaMalmyzh), pnpm, standalone-сборка в CI по флагу `STANDALONE_BUILD=1`.
- **Контент:** 128 страниц стадии 1 живут в Payload (коллекция `pages`, HTML-поле) + `faq` + `zayavki` (формы). Правки — в `/admin`. Сид: `src/seed/seedFromStage1.ts`.
- **GO владельца на стадию 2 получен 2026-07-17** (третий жилец на том же боксе, решение владельца).
- Контент-конвейер: `pnpm parse-mirror` → `content/pages.json`, `pnpm fetch-images` (повторный харвест rmz43.ru).
- **Картинки контента → Медиа** (аудит §4.2): `corepack pnpm payload run src/seed/mediaFromContent.ts` — dry-run, `APPLY=1` применяет. Импортирует харвест-файлы из `public/images/wp/**` в коллекцию `media` **1:1** (варианты размеров WP не схлопываем в оригинал — среди них реальные кропы; замер в `docs/AUDIT-rmz43.md` §4.2) и переводит ссылки контента на `/media/<id>/<имя>` — URL по **номеру записи**, поэтому замена файла в админке не ломает ссылки. Идемпотентно; статика `public/images/wp/**` остаётся исходником импорта и страховкой. Гейт — job `media-import` в CI (полный прогон на эфемерной БД + живая выдача картинки через `next start`): локально проверить нельзя, на PC40 нет ни Postgres, ни Docker. Применение на проде — воркфлоу **`apply-media-import.yml`** (dispatch): `dry-run` по умолчанию, для `apply` нужно вписать слово `ПРИМЕНИТЬ`; сам делает `pg_dump` в `shared/backups/` до первой записи. Пишет в прод-БД через SSH-туннель к 5432 бокса (полный toolchain есть только на раннере), файлы едут `rsync` в `shared/media`. Порядок фаз обязателен: `PHASE=media` → доставка файлов (tar-over-ssh, rsync на боксе нет) → `PHASE=links`, иначе будет окно, где страницы ссылаются на не приехавший файл. Если доставка упала и остались записи без файлов — прогон падает с диагнозом, лечение только `mode=reset` (снос импортных `wp-`записей) и чистый повтор: перезалить файл в ту же запись нельзя, Payload добавит суффикс к имени и сломает поиск по детерминированному имени. `reset` сам откажется работать, когда контент уже переведён на `/media/` — там откат только через `pg_dump`.
- **Каталог сервисов Малмыжа** (постулат 37): мы записаны в `вход.вмалмыже.рф/services`, кнопка «Сервисы Малмыжа» — в шапке и подвале обоих интерфейсов (`ECOSYSTEM` в `src/lib/site.ts`, URL строго punycode — G133/G134). Пользовательского входа у нас нет → требование «авторизация только через ЕСА» n/a до первого посетительского аккаунта; админка Payload — вход персонала, под требование не попадает.
- **Контентные страницы рендерятся строго в рантайме** (`export const dynamic = "force-dynamic"`): `/`, `/novosti/`, `/voprosy-i-otvety/`, `/new/`, `llms.txt`, `sitemap.xml`. Причина: CI собирает с пустой БД (прод не тянет `build`, G20), и при `revalidate` сборка запекала пустые страницы — главная шла **без контентного блока час после каждого деплоя**, sitemap пустой. Вторая причина: ссылки `/media/<id>/…` привязаны к конкретной базе, поэтому пререндер на сборочной БД был бы не устаревшим, а прямо неверным (и «засеять сборочную БД» — не лечение). Маршрут `[...slug]` этим не страдал: без `generateStaticParams` он генерится по первому запросу. Гейт — шаг «Контентные страницы рендерятся из БД» в CI: собрать на пустой базе, наполнить, поднять `next start`, потребовать контент в ответе.
- План улучшений и аудит: `docs/AUDIT-rmz43.md`.

## Гейты и деплой

- Гейты перед PR: `corepack pnpm lint && corepack pnpm typecheck && corepack pnpm build` (build требует локальный Postgres — `.env.example`).
- Мерж в `main` → **авто-деплой на прод** (`deploy-prod.yml`: standalone-сборка в CI с эфемерным Postgres, tar → releases → symlink → `systemctl restart rmz`) + смоук #011 (Node :3002 → 200, маркер «Малмыжский», /admin, XFP-301).
- Первичная подготовка бокса — `setup-prod-stage2.yml` (dispatch: БД+роль, /etc/rmz/rmz.env, юнит, restore сида, админ). Будущие миграции схемы — вручную ДО деплоя (guard #017), паттерн apply-migration Сабантуя.
- **Миграции Payload/Drizzle — правило снапшота (G192).** Применение — только `apply-migration.yml` (dry-run в транзакции → apply с `pg_dump`), ДО мержа кода. Порядок: **`.json`-снапшот коммитится вместе с каждой миграцией** (без снапшота Drizzle считает БД пустой и генерирует правдоподобный `down()` с `DROP TABLE ... CASCADE` по всем таблицам), `down()` читается глазами до применения, `up()` прогоняется на чистой БД. Первый `migrate:create` даёт полную схему законно — опасен второй. Заодно: env-файл прода `/etc/rmz/rmz.env` имеет права 600 root — читать в воркфлоу только через `sudo -n` (у вМалмыже голый `grep` уронил `apply-migration.yml`).
- ⚠️ С dev-машины PC40 SSH на бокс режется на banner exchange — вся диагностика прода **только через Actions** (`probe-prod.yml`).
- PR-flow: ветка → PR → squash-merge. Прямых пушей в `main` нет.
- **Репозиторий публичный, `main` защищён сервером** (D-030): прямой и force-push в `main`
  отклоняет сервер (`enforce_admins`), **обязательные проверки `build`, `media-import`,
  `gitleaks` блокируют влитие на сервере**. Мерж — `gh pr checks <PR> --watch`, затем
  `gh pr merge --squash --delete-branch`. Авто-мерж (`--auto`) в настройках репо выключен и
  отвечает ошибкой — не рассчитывать на него как на ожидание гейта.
- **Секреты**: PR-гейт `secret-scan` (gitleaks, полная история, `--redact`). У публичного
  репо утёкший секрет скомпрометирован с секунды пуша — лечится ротацией, а не удалением
  коммита.

### Автономия под гейтами — авто-мерж без человеческого «окей» (cross-project mandate #027)

Владелец убрал ритуал «окей на дифф/мерж/деплой»: подтверждение **заменено автоматическими гейтами**. Для Claude Code механизм закреплён в коммитимом `.claude/settings.json` (`defaultMode: auto` + allow/deny); остальные агенты обязаны соблюдать те же границы. Ярусно по риску:

| Операция | Режим | Гейт = подтверждение |
|---|---|---|
| Правки файлов, ветка, коммит, push ветки, PR, **авто-мерж** | **авто** | `corepack pnpm build` зелёный (build = typecheck + статический экспорт) **и** CI зелёный |
| **Деплой на прод** (авто-триггер от merge; для docs/`.claude` не срабатывает — `paths-ignore`) | **авто** | смоук #011 в воркфлоу (200 + маркер + XFP-301); после деплоя дождаться зелёного |
| `probe-prod.yml` (read-only диагностика) | **авто** | — |
| `apply-authorize-key.yml` (SSH-ключи бокса) | **`ask`** | человек сверяет |
| Необратимые операции с прод-данными/инфрой вне воркфлоу | **подтверждать** (#025) | черта не пересекается |

Обязанности: красный гейт → НЕ мержить, чинить; после merge ждать единственный авто-деплой (`concurrency: deploy-prod`); зелёный пайплайн ≠ корректный результат — по возможности глянуть прод глазами (#011).

**deny (жёсткий блок):** прямой/force push в `main`.

## 🚫 Чужие репо — строго read-only (ADR-0009, постулат 35)

Решение владельца 2026-07-18, симметрично для всех проектов экосистемы: **подсматривать можно — трогать нельзя.**

- **Читать** код, доки и почту любого sibling-репо (`../brain_matrica/`, `../SabantuyMalmyzh/`, `../KazanskayaMalmyzh/`, любой другой) — можно и нужно (ADR-0007, тактические чтения).
- ❌ **Не изменять, не создавать, не удалять, не коммитить, не двигать** файлы и git-состояние **никакого** чужого репо: ни `git add/commit/push`, ни `checkout`/`pull`/`stash`, ни правки файлов.
- **Услуги соседей — только через опубликованные интерфейсы** (реестр: `../brain_matrica/access/INDEX.md` §5): vault KARMAN по API, VK-контент через шлюз SARAFAN, авторизация посетителей — ЕСА `вход.вмалмыже.рф`. Чужой репо — подсказка, а не место исполнения.
- Наш собственный канал наружу — только `mailbox/to-brain/` в этом репо (см. ниже).

## 📬 Mailbox check — ДО любой другой работы (ADR-0001 v3)

Асимметричные mailbox'ы: каждая сторона пишет только в свой репо.

| Направление | Кто пишет | Где |
|---|---|---|
| `brain → RmzMalmyzh` | brain | `../brain_matrica/mailboxes/RmzMalmyzh/from-brain/*.md` — только **читаем**: локальная копия + GitHub API (`gh api repos/Valstan/brain_matrica/contents/…`), без синхронизации чужого репо |
| `RmzMalmyzh → brain` | мы | **`mailbox/to-brain/*.md`** в этом репо (через PR) |

Сканить только корень `from-brain/` (не `DRAFTS/`, не `ARCHIVE/`). Compliance: `mandate`→MUST (применить или feedback-письмо с блокером), `recommend`→SHOULD (применить или обоснованный отказ письмом), `suggest`→MAY. Письма без `compliance`: `directive`→MUST, `idea`→SHOULD. ❌ Никогда не писать/коммитить в `../brain_matrica/` — частный случай ADR-0009 (см. выше).

Молчание в ответ на письмо brain обязан читать как «не дошло» и эскалировать (постулат 38). Поэтому осознанное «потом» тоже оформляется письмом в три строки: что прочитано, почему отложено, триггер возврата.

Формат исходящего письма `mailbox/to-brain/YYYY-MM-DD-slug.md`:

```yaml
---
from: RmzMalmyzh
to: brain
date: YYYY-MM-DD
topic: ...
kind: idea | question | feedback | report
compliance: suggest | recommend | mandate   # для kind=idea
urgency: low | normal | high
---
```

## Session-память

- `docs/SESSION_HANDOFF.md` — статус/нитка/следующий шаг (обновляет `/close_session`, читает `/start`).
- `docs/PENDING_FOLLOWUPS.md` — отложенные пункты с decay-триажем (#033).
- Хук `SessionStart` (`.claude/scripts/session_start.sh`, D-066 / pool #268) печатает
  `git status`, последние коммиты и handoff в контекст при открытии сессии. Только чтение,
  без сети: `pull` остаётся осознанным шагом `/start`.
- **Handoff пишется по шагам нитки, не только при закрытии:** сделал шаг, смержил PR —
  обновил «где мы» и «что дальше» в том же PR. Сессия = одна нитка, не один день;
  `/close_session` — страховка, не обязанность.
- **После обрыва** (D-066, команда `/obriv` снята): восстанавливай состояние из
  Git/PR/handoff, а не из памяти чата: `git status`, `git log`, `gh pr list`, `gh run list`.
  Грабля харнесса — битая запись файла: исходник как `Bin` в `git diff --cached --stat`
  означает NUL-байты, файл вычистить и пересохранить UTF-8.

## 🚫 Текст не сочиняется внутри команды (D-046, мандат владельца 2026-08-25)

> **Не сочиняй текст внутри командной строки. Никогда.** Пиши содержимое инструментом
> записи файлов, а команде отдавай **путь** — один ASCII-токен.

Heredoc не глючит — он не снимает ни одного слоя экранирования: кавычки аргумента
инструмента → локальный шелл (на Windows их два, G190) → удалённый шелл при `ssh` (G30) →
кодовая страница консоли (G191). Отказ выглядит как ошибка в коде, а не в доставке текста.

| Задача | Вместо | Правильно |
|---|---|---|
| Скрипт / патч / конфиг | `cat <<'EOF' > f` | записать файл инструментом записи |
| Commit message | `-m` с переносами | файл → `git commit -F msg.txt` |
| PR / issue body | `--body "…"` | файл → `gh pr create --body-file body.md` |
| SQL на проде | `psql -c "…"` | файл → `scp` → `psql -f q.sql` |
| HTTP-тело | heredoc в `curl` | файл → `curl --data-binary @body.json` |
| Файл на удалённый хост | heredoc через `ssh` | `scp`, либо `base64 -w0 f \| ssh host 'base64 -d > /path'` |

**Триггер:** в собственной команде появился перенос строки, backtick, `$`, кавычка или
не-ASCII — остановись и перепиши через файл, не экранируй. Однострочные ASCII-команды
и короткие ASCII `-m` разрешены. Heredoc внутри `run:` наших YAML-воркфлоу под правило не
попадает: это уже файл, парсер один.

## Git и совместная работа агентов

- Один агент — одна задача — отдельная ветка. Параллельные пишущие агенты работают
  только в отдельных `git worktree`; два агента не пишут в одно рабочее дерево.
- Перед правкой проверяй `git status`. Незнакомые изменения считай чужими: не удаляй,
  не форматируй попутно, не включай в свой коммит и не прячь в stash.
- Если границы задач пересекаются, второй агент ждёт merge первого и обновляет свою
  ветку до начала правок.
- Git/PR, `docs/SESSION_HANDOFF.md`, mailbox и ADR — межмодельная память проекта.
  Чат одной модели не является источником истины для другой. После обрыва сначала
  восстанавливай фактическое состояние из этих артефактов.

## Команды

- Памятки лежат в `.claude/commands/`, но их workflow применим любому агенту.
  `allowed-tools:` можно игнорировать; `/команда` означает «выполни шаги файла»;
  `AskUserQuestion: «…»` означает «задай вопрос и дождись явного ответа» — форма
  может отличаться, обязательный шаг и предохранитель сохраняются.
- `/start` — синхра + mailbox-check + handoff.
- `/close_session` — сохранить состояние, всё на origin через PR.
- `/obriv` снята решением владельца D-066 (2026-09-02): восстановление после обрыва —
  см. §Session-память. Точка возврата — тег `pre-fable-freeze-2026-09-02`.
