# Стадия 2: sharp нужен Payload'у (обработка загрузок), esbuild — Payload CLI.
allowBuilds:
  esbuild: true
  sharp: true
  unrs-resolver: false # пребилт-бинарь ставится optional-депом, postinstall-фолбэк не нужен
