# SESSION_HANDOFF — RmzMalmyzh

Updated: 2026-07-28

## Статус

**Стадия 2 LIVE, картинки контента управляются из админки, новый интерфейс v2 в превью.** https://рмз.вмалмыже.рф/ — Next.js 15 + Payload CMS 3.75 + PostgreSQL, третий Node-жилец Бокса Сабантуя (systemd `rmz`, :3002, `MemoryMax=512M`). Формы боевые (SmartCaptcha + SMTP). **Все 180 картинок контента живут в коллекции `media`**, 521 ссылка 128 страниц переведена на стабильные URL — редактор меняет картинку в `/admin`, и она обновляется на всех страницах. Новый интерфейс — https://рмз.вмалмыже.рф/new/ (noindex, превью для руководства).

## Текущая нитка

2026-07-28: закрыт долг по почте brain (три мандата), затем взят пункт аудита §4.2 — переезд картинок контента в Медиа. Попутно найден и исправлен тихий прод-дефект: главная и sitemap пустовали час после каждого деплоя. PR #25–#31.

**Блокер нитки не в коде:** ждём решения руководства по v2. Вопрос сформулирован владельцу через brain как выбор из двух с ценой каждого (письмо `2026-07-28-ack-adr0009-services-card-g192.md`).

## Сделано (последние сессии)

- **2026-07-28 (эта сессия):**
  - **Почтовый долг brain закрыт (PR #25).** Мандат **ADR-0009** (взаимный read-only чужих репо) висел без ack с 18.07 — единственная 🔴 портфеля; технического блокера не было, сессию просто не открывали. Правило вшито отдельной секцией в `CLAUDE.md` **выше** mailbox-раздела + продублировано в `AGENTS.md` (entry point для Codex, впервые заведён в git). Мандат **каталога сервисов**: кнопка «Сервисы Малмыжа» в шапке, мобильном меню и подвале **обоих** интерфейсов, URL в константе `ECOSYSTEM` строго в punycode (G133/G134); карточка отправлена письмом. **G192**: правило снапшота миграций вшито в `CLAUDE.md` **до** появления `migrations/`. По просьбе brain проверен env-контур — мины вМалмыже у нас нет, все чтения `/etc/rmz/rmz.env` идут внутри `ssh rmz 'sudo bash -s'`, то есть root-шеллом.
  - **Картинки контента → Медиа (PR #26–#30), аудит §4.2.** Было: 180 файлов статикой, 521 ссылка, в Медиа 0 записей, редактор бессилен. Стало: импорт **1:1** (варианты размеров WP не схлопнуты — измерил все 180 файлов, 34 из 76 реальные кропы до 51% разницы аспекта), ссылки на `/media/<id>/<имя>` — по **номеру записи**, а не по имени файла, потому что Payload при замене файла меняет `filename` и ссылки по имени отвалились бы разом. Скрипт `src/seed/mediaFromContent.ts` (фазы `media`/`links`/`reset`, dry-run по умолчанию), роут `src/app/media/[id]/[name]/route.ts`, воркфлоу `apply-media-import.yml`. Применено на проде: `media=180`, legacy-ссылок 0, картинки отдаются `200 image/*`.
  - **Прод-дефект, найденный попутно (PR #31).** Главная и `sitemap.xml` пустовали **час после каждого деплоя**: CI собирает с пустой БД, а `revalidate` запекал пустой пререндер. Шесть статических маршрутов переведены на `dynamic = "force-dynamic"`. Проверено на проде сразу после деплоя: контентный блок есть, 68 ссылок `/media/`, 129 адресов в sitemap.
  - **Письмо мозгу:** пять переносимых грабель (`2026-07-28-media-import-five-gotchas.md`), из них пункт 1 просим проверить у Казанской — у неё тот же стек и тот же контур сборки.
- **2026-07-18:** оба owner-gate стадии 2 (SmartCaptcha + SMTP) и новый интерфейс v2 на `/new/` с анкетой соискателя. PR #18–#23. Грабля: пароль приложения Яндекса обязан быть создан **под аккаунтом ящика** (иначе 535, неотличимо от неверного пароля).
- Ранее: стадия 2 на проде (PR #13–#17), стадия 1 (копия 128 страниц, DNS, TLS, CI-деплой), SEO quick wins, alt для картинок, FAQ.

## Следующий шаг

**Ждём решения руководства по v2** — единственный внешний блокер. Возможные исходы:

1. **Одобрено → переезд v2 на корень:** перенести `src/app/(v2)/new/*` в корень группы, снять `robots: { index: false }` в `(v2)/layout.tsx` и `Disallow: /new/` в `src/app/robots.ts`, решить судьбу старого `(frontend)`, обновить `NAV`/sitemap. **Перед этим** перенести в v2 недостающие разделы старого сайта (о предприятии, продукция, закупки, галерея — сейчас v2 ссылается на страницы старого интерфейса).
2. **Правки по итогам показа** — итеративно на `/new/`, без риска для основного сайта.

Кодовые кандидаты поменьше (`docs/AUDIT-rmz43.md` §4.2): Яндекс Метрика с фильтрами (G136, ☐); схема Product/Service для каталога двигателей.

## Открытые вопросы для владельца

- **Решение руководства по v2** — основной блокер (см. «Следующий шаг»).
- **`AGENTS.md` впервые заведён в git** (PR #25). Файл лежал untracked с 20.07, в `.gitignore` его не было — как и `.agents/`, то есть намеренного игнора не было, просто не коммитили. Завёл, чтобы правило ADR-0009 доехало до Codex-сессий на других машинах. Если так не задумано — откатывается одним коммитом. Зеркала команд `.agents/skills/*` оставил untracked.
- **Сменить временный пароль админки** `/admin` (`m.zavod@rmz43.ru`) — владелец сказал «зайду и сделаю».
- **e2e-проверка почты** — живое письмо глазами ещё не подтверждено (журнал чист, 535 после смены пароля нет).
- Хвост: в `/admin` → Заявки лежат тестовые записи («ТЕСТ (от Claude, можно удалить)» и анкета «Токарь / Тестов Тест Тестович») — можно удалить.

## Прод-заметки (стадия 2)

- **Картинки контента:** применение импорта — только `apply-media-import.yml` (dispatch). `dry-run` безопасен, для `apply`/`reset` нужно вписать слово `ПРИМЕНИТЬ`; воркфлоу сам делает `pg_dump` в `shared/backups/` (ротация 10). Порядок фаз обязателен: `media` → доставка файлов → `links`; необратимый шаг последний, поэтому падение середины оставляет прод рабочим (проверено — на этом и упало 28.07). Файлы едут **tar-over-ssh**: `rsync` на боксе не установлен.
- **Записи media без файлов** (не доехала доставка) — прогон падает с диагнозом. Лечение только `mode=reset` + чистый повтор: перезалить файл в ту же запись нельзя, Payload добавит суффикс к имени и сломает поиск по детерминированному имени. `reset` откажется работать, когда контент уже переведён на `/media/` — там откат через `pg_dump`.
- **Проба REST API:** URL писать **со слэшем** и с `curl -L` — при `trailingSlash: true` запрос без слэша отдаёт 301 с телом-путём вместо JSON, и `grep` без совпадений под `set -e` роняет шаг молча. За сессию наступили дважды.
- Секреты форм: **только** через `apply-form-secrets.yml` (dispatch), не руками. `SMTP_PASS` опционален.
- ⚠️ Пароль приложения Яндекса создавать **под аккаунтом ящика** `valstan@valstan.ru` — иначе `535`.
- `setup-prod-stage2.yml` с `force_restore=true` **сносит контент** — только осознанно.
- Будущие изменения схемы Payload → миграция + применение на проде ДО деплоя (guard #017) + **`.json`-снапшот в том же коммите** (G192, `migrations/` пока нет).
- Postgres на боксе **нативный** (`sudo -u postgres psql`, `localhost:5432`); `docker exec … psql` в воркфлоу — это CI-сервис, не бокс. Прогнать `payload run` против прода можно через туннель `ssh -f -N -L 15432:127.0.0.1:5432 rmz`.
- Локальная разработка требует Postgres (`.env.example` → `.env`). **На PC40 его нет и Docker тоже нет** → локальный `pnpm build` всегда падает на prerender (`ECONNREFUSED`), это не красный гейт: build закрывает CI с эфемерным PG 17.
