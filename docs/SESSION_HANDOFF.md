# SESSION_HANDOFF — RmzMalmyzh

Updated: 2026-07-18

## Статус

**Стадия 2 LIVE**: https://рмз.вмалмыже.рф/ — Next.js 15 + **Payload CMS 3.75 + PostgreSQL**, третий Node-жилец Бокса Сабантуя (systemd `rmz`, :3002, `MemoryMax=512M`) рядом с `sabantuy` (:3000) и `kazanskaya` (:3001). Контент (128 страниц + FAQ) живёт в БД, правится в `/admin` **без деплоя**. Формы пишут заявки в CMS. Последний деплой — run 29615053034, смоук зелёный, прод проверен глазами (главная, услуги, FAQ, /admin, sitemap, llms.txt) + e2e-заявка через прод-форму прошла.

## Текущая нитка

2026-07-17/18, сессия стадии 2 (GO владельца получен, решение владельца: «подселить третий сайт рядом с Сабантуем и Казанской, разделить в nginx»). Один большой PR #13 + три фикса CI (#14–#16).

## Сделано (последние сессии)

- **2026-07-17/18 (эта сессия) — стадия 2 на проде:**
  - PR #13: Payload 3.75 + PG по образцу KazanskayaMalmyzh (ADR-0007). Коллекции `pages` (128 страниц стадии 1, HTML-поле), `faq`, `media`, `users`, `zayavki`. Фронт (`[...slug]`, главная, `/novosti/`, FAQ, sitemap, llms.txt) переведён с `content/pages.json` на Payload local API: ISR 1 ч + on-demand revalidate из хуков коллекций. Сид `src/seed/seedFromStage1.ts` (идемпотентный).
  - PR #13: бэкенд форм — `/api/zayavka` (honeypot + SmartCaptcha по ключам + email-дубль по SMTP), замена mailto стадии 1; при сетевой ошибке фронт предлагает mailto-fallback.
  - PR #13: деплой-конвейер — standalone-артефакт из CI → `releases/<sha>` → `systemctl restart rmz`; nginx-vhost переключается на proxy `:3002` **только после 200 от Node** (иначе прод остаётся на статике стадии 1); смоук #011 + `/admin`. Новый `setup-prod-stage2.yml` (dispatch, идемпотентный): роль/БД `rmz` на боксе (пароли генерятся **на боксе**), `/etc/rmz/rmz.env`, юнит, restore сида из CI-дампа, админ Payload. `ci.yml`: + typecheck + PG-сервис.
  - Фиксы CI: #14/#15 — `pg_dump` через `docker exec` сервис-контейнера (раннер несёт client 16 против PG 17); #16 — **top-level await вместо `void run()`** (`payload run` завершал процесс до конца сида → на прод уехала пустая БД) + страховки `pages >= 100` в setup-воркфлоу.
  - Админ Payload: `m.zavod@rmz43.ru`, временный пароль выдан владельцу в чате — **владелец должен сменить его в `/admin`**.
  - Письмо мозгу: `mailbox/to-brain/2026-07-18-payload-run-silent-noop.md` (грабля `payload run`, два скрипта Сабантуя с тем же паттерном).
- Ранее: стадия 1 (копия 128 страниц, DNS, TLS, CI-деплой), SEO quick wins, alt для 397 картинок, FAQ-страница.

## Следующий шаг

**Новый UI — единственная крупная нитка стадии 2, которая осталась** (сейчас на проде намеренная копия старого визуала rmz43.ru). Начинать с решения владельца по дизайну: полный редизайн или поэтапное осовременивание текущей сетки.

Кодовые кандидаты поменьше (из `docs/AUDIT-rmz43.md` §4.2, статусы там же):
- Каталог «двигатель → услуги → запчасти» со схемой Product/Service (☐).
- Яндекс Метрика с фильтрами (G136) (☐).
- Загрузка картинок контента в Media-коллекцию (сейчас харвест-картинки лежат статикой в `public/images/wp`, редактор не может заменить их из админки).

Фон (некодовое): Яндекс Бизнес / 2ГИС (§3.6) — задача владельца; вычитка FAQ владельцем.

## Открытые вопросы для владельца

- **Ключи SmartCaptcha** (Яндекс Облако) — код готов, включается через env; пока форму держит только honeypot.
- **SMTP-доступы** — пока заявки видны только в `/admin`, письма в отдел сбыта не уходят.
- **Сменить временный пароль админки** после первого входа.
- Решение по новому UI (см. «Следующий шаг»).

## Прод-заметки (стадия 2)

- Тестовая заявка «ТЕСТ (от Claude, можно удалить)» лежит в `/admin` → Заявки — можно удалить.
- Порядок ручного вмешательства в прод-БД: `setup-prod-stage2.yml` с `force_restore=true` **сносит контент** и заливает сид заново — использовать только осознанно.
- Будущие изменения схемы Payload → миграция + применение на проде ДО деплоя (guard `#017` в `deploy-prod.yml`, паттерн `apply-migration.yml` Сабантуя).
- Локальная разработка требует Postgres: `.env.example` → `.env`; после добавления коллекции — `corepack pnpm payload run src/seed/pushSchema.ts`.
