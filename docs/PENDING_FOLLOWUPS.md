# PENDING_FOLLOWUPS — отложенное (re-триаж в /start, pool #033)

Формат пункта:

```
- [ ] <что> — added: YYYY-MM-DD, snoozed: 0, last-touch: YYYY-MM-DD
```

- [x] **ПОПРАВКА 2026-08-04 — гейт идентичности `CLAUDE.md` ↔ `AGENTS.md` отменён.** ADR-0011 изменил постановку: `AGENTS.md` теперь единственный канон, а `CLAUDE.md` — тонкий vendor-адаптер. Поддерживать две полные копии больше не нужно; такой гейт краснел бы на правильном состоянии и подталкивал к возврату вредного дублирования. Исходный пункт закрыт по mandate `2026-07-31-adr-0011-check-4-and-your-mirror-gate-is-obsolete`. — added: 2026-07-28, closed: 2026-08-04

- [ ] **Схема Product/Service для каталога «двигатель → услуги»** (`docs/AUDIT-rmz43.md` §4.2, хвост пункта про каталог). Имеет смысл делать **после** решения по v2: если каталог переезжает на корень, разметка ставится сразу на боевые URL, иначе придётся переделывать под `/new/`. **Триггер возврата:** решение руководства по v2. — added: 2026-07-28, snoozed: 0, last-touch: 2026-07-28

- [ ] **Идея #098 от brain — мерить чистую часть без стенда.** Каскадная анкета соискателя (11 шагов, логика пропусков и «— не указано —») и `src/lib/v2/catalog.ts` (6 групп, 33 модели) — чистые функции, их можно проверить числами без поднятия сервера и БД. Актуально тем, что у нас **нет локального стенда вообще** (на PC40 ни Postgres, ни Docker), и каждая проверка стоит прогона CI на 3 минуты. **Триггер возврата:** следующая правка анкеты или каталога. — added: 2026-07-28, snoozed: 0, last-touch: 2026-07-28

- [ ] **Vault ADR-0012, сценарий A — сначала наполнить канал восстановления.** Клиент не внедрён сознательно: комната `rmz` подтверждённо содержит зеркало deploy-ключа, но нет подтверждённых `DATABASE_URL`/`PAYLOAD_SECRET`, а отдельный `/etc/rmz/secrets-token.env` ещё не подключён к systemd. Клиент в таком состоянии прошёл бы unit-тест, но не восстановил бы сервис при потере `/etc/rmz/rmz.env`. **Триггер возврата:** отдельный безопасный workflow зеркалирования allowlist рантайм-секретов из бокса + доставки bootstrap-токена; затем негативный прогон ключа вне allowlist и recovery-проверка в окне. — added: 2026-08-04, snoozed: 0, last-touch: 2026-08-04

- [ ] **keep-alive / `headersTimeout` — осознанно не делаем (G234, письмо brain 2026-08-08, `recommend`).** Замер 2026-08-22 через `probe-prod.yml`: у `rmz.conf` **нет `upstream`-блока с `keepalive N`**, `proxy_set_header Connection` не задан (дефолт `close`), а `keepalive_timeout 65` в `nginx.conf` — клиентская сторона. Без idle-пула прокси не может отправить запрос в закрытый сокет, то есть гонки не существует. Вторая половина корня закрыта самим Next: `start-server.js` уже вешает SIGINT/SIGTERM на `cleanup` с `server.close()`, юнит согласован (`KillSignal=15`, `TimeoutStopUSec=1min30s`). ⚠️ Когда вернёмся: Next standalone читает `KEEP_ALIVE_TIMEOUT` из окружения, но **`headersTimeout` не выставляет вообще** (дефолт Node 60000) — рецепт «65000/66000» одними переменными юнита не исполняется, нужен враппер поверх `server.js`. **Триггер возврата:** появление `upstream { keepalive N }` в `rmz.conf` **или** первая серия POST от Сарафана (D-015). — added: 2026-08-22, snoozed: 0, last-touch: 2026-08-22

- [ ] **Рубрикатор ленты новостей — автоклассификатор, а не ручная простановка.** Сейчас рубрику ставит редактор в админке, и это осознанно: у Калининского СДК автоклассификатор работает потому, что их сообщество метит посты хештегами **и** владелец руками разметил эталон из 29 постов, по которому классификатор проверяется тестом. У нас нет ни того, ни другого — в постах стены РМЗ, прочитанных глазами, хештегов не видно. Правила, скопированные без эталона, дадут правдоподобный мусор, который нечем проверить. **Триггер возврата:** получен `GATEWAY_KEY_RMZ` → снят полный дамп стены (`wall.get owner_id=-195583920`) → посчитаны доля постов с хештегами, топ-30 хештегов, доля репостов и постов без текста → владелец разметил ≥30 постов рубриками, которые сам назовёт. Только после этого писать классификатор — с флагом уверенности и в режиме черновиков. — added: 2026-08-23, snoozed: 0, last-touch: 2026-08-23

- [ ] **`.ts` + `.json` для миграции `20260823_000001` (правило G192).** В ветке `feat/novosti-collection` лежит только `.sql` — этого достаточно, чтобы применить схему, но недостаточно для **следующей** миграции: без `.json`-снапшота Drizzle считает базу пустой и генерирует правдоподобный `down()` с `DROP TABLE ... CASCADE` по всем таблицам. Пара получается прогоном `payload migrate:create` на пустой базе в CI. **Триггер возврата:** до начала второй миграции схемы, что бы её ни вызвало. — added: 2026-08-23, snoozed: 0, last-touch: 2026-08-23
