# PENDING_FOLLOWUPS — отложенное (re-триаж в /start, pool #033)

Формат пункта:

```
- [ ] <что> — added: YYYY-MM-DD, snoozed: 0, last-touch: YYYY-MM-DD
```

- [x] **ПОПРАВКА 2026-08-04 — гейт идентичности `CLAUDE.md` ↔ `AGENTS.md` отменён.** ADR-0011 изменил постановку: `AGENTS.md` теперь единственный канон, а `CLAUDE.md` — тонкий vendor-адаптер. Поддерживать две полные копии больше не нужно; такой гейт краснел бы на правильном состоянии и подталкивал к возврату вредного дублирования. Исходный пункт закрыт по mandate `2026-07-31-adr-0011-check-4-and-your-mirror-gate-is-obsolete`. — added: 2026-07-28, closed: 2026-08-04

- [ ] **Схема Product/Service для каталога «двигатель → услуги»** (`docs/AUDIT-rmz43.md` §4.2, хвост пункта про каталог). Имеет смысл делать **после** решения по v2: если каталог переезжает на корень, разметка ставится сразу на боевые URL, иначе придётся переделывать под `/new/`. **Триггер возврата:** решение руководства по v2. — added: 2026-07-28, snoozed: 3, last-touch: 2026-09-14 (всплывал 02.09, 04.09 и 14.09. 14.09 снят с повестки внешним фактом: сайт остановлен, работа над проектом приостановлена — разметка на выключенных страницах бессмысленна. Триггер уточнён: resume сайта И решение по v2)

- [x] **✅ 2026-09-03 — закрыт первым применением: `pnpm test` (node --test, типы снимает Node) гоняет чистую логику ленты `src/lib/novosti/ingest.ts` на фикстуре без БД, шаг стоит в CI до сборки. Анкета и каталог по тому же рецепту — при следующей их правке.** Исходно: **Идея #098 от brain — мерить чистую часть без стенда.** Каскадная анкета соискателя (11 шагов, логика пропусков и «— не указано —») и `src/lib/v2/catalog.ts` (6 групп, 33 модели) — чистые функции, их можно проверить числами без поднятия сервера и БД. Актуально тем, что у нас **нет локального стенда вообще** (на PC40 ни Postgres, ни Docker), и каждая проверка стоит прогона CI на 3 минуты. **Триггер возврата:** следующая правка анкеты или каталога. — added: 2026-07-28, snoozed: 1, last-touch: 2026-09-02 (всплыл в /start 02.09, без ответа владельца)

- [x] **✅ 2026-09-04 — сделано: шаг «Воркфлоу — валидный YAML» стоит в `ci.yml` первым после checkout, красный проверен контролем на историческом битом файле (`git show 94c9ed2:.github/workflows/apply-migration.yml` → «mapping values are not allowed here»).** Исходно: **Гейт валидности воркфлоу: `yaml.safe_load` по `.github/workflows/*.yml` в CI.** 02.09 `apply-migration.yml` два раза «прошёл» через PR (#52, #53) невалидным: второе двоеточие в `name:` шага. GitHub не краснит такой файл в PR — он просто показывает воркфлоу по пути, а dispatch отвечает 422 «нет триггера workflow_dispatch». Гейт бесплатный: один шаг в `ci.yml`, красный на исходном файле воспроизводим (`git show 94c9ed2:.github/workflows/apply-migration.yml`). **Триггер возврата:** следующая правка `ci.yml`. — added: 2026-09-02, snoozed: 0, last-touch: 2026-09-02

- [ ] **Vault ADR-0012, сценарий A — сначала наполнить канал восстановления.** Клиент не внедрён сознательно: комната `rmz` подтверждённо содержит зеркало deploy-ключа, но нет подтверждённых `DATABASE_URL`/`PAYLOAD_SECRET`, а отдельный `/etc/rmz/secrets-token.env` ещё не подключён к systemd. Клиент в таком состоянии прошёл бы unit-тест, но не восстановил бы сервис при потере `/etc/rmz/rmz.env`. **Триггер возврата:** отдельный безопасный workflow зеркалирования allowlist рантайм-секретов из бокса + доставки bootstrap-токена; затем негативный прогон ключа вне allowlist и recovery-проверка в окне. — added: 2026-08-04, snoozed: 2, last-touch: 2026-09-14 (всплыл 04.09 и 14.09. 14.09: сайт остановлен, сценарий восстановления сервиса временно не имеет предмета — но и цена простоя при потере env-файла теперь нулевая. Возвращаться вместе с resume)

- [ ] **keep-alive / `headersTimeout` — осознанно не делаем (G234, письмо brain 2026-08-08, `recommend`).** Замер 2026-08-22 через `probe-prod.yml`: у `rmz.conf` **нет `upstream`-блока с `keepalive N`**, `proxy_set_header Connection` не задан (дефолт `close`), а `keepalive_timeout 65` в `nginx.conf` — клиентская сторона. Без idle-пула прокси не может отправить запрос в закрытый сокет, то есть гонки не существует. Вторая половина корня закрыта самим Next: `start-server.js` уже вешает SIGINT/SIGTERM на `cleanup` с `server.close()`, юнит согласован (`KillSignal=15`, `TimeoutStopUSec=1min30s`). ⚠️ Когда вернёмся: Next standalone читает `KEEP_ALIVE_TIMEOUT` из окружения, но **`headersTimeout` не выставляет вообще** (дефолт Node 60000) — рецепт «65000/66000» одними переменными юнита не исполняется, нужен враппер поверх `server.js`. **Триггер возврата:** появление `upstream { keepalive N }` в `rmz.conf` **или** первая серия POST от Сарафана (D-015). — added: 2026-08-22, snoozed: 0, last-touch: 2026-08-22

- [ ] **Рубрикатор ленты новостей — автоклассификатор, а не ручная простановка.** Сейчас рубрику ставит редактор в админке, и это осознанно: у Калининского СДК автоклассификатор работает потому, что их сообщество метит посты хештегами **и** владелец руками разметил эталон из 29 постов, по которому классификатор проверяется тестом. У нас нет ни того, ни другого — в постах стены РМЗ, прочитанных глазами, хештегов не видно. Правила, скопированные без эталона, дадут правдоподобный мусор, который нечем проверить. **Триггер возврата:** ~~получен `GATEWAY_KEY_RMZ`~~ (✅ 02.09: ключ в `/etc/rmz/rmz.env` как `SARAFAN_GATEWAY_KEY`, стена читается, 178 постов) → снят полный дамп стены (`wall.get owner_id=-195583920`) → посчитаны доля постов с хештегами, топ-30 хештегов, доля репостов и постов без текста → владелец разметил ≥30 постов рубриками, которые сам назовёт. Только после этого писать классификатор — с флагом уверенности и в режиме черновиков. — added: 2026-08-23, snoozed: 0, last-touch: 2026-08-23

- [ ] **Миграции: сверять полную схему, а не перечислять таблицы регуляркой.** `dump-schema.yml` отдаёт DDL только тех таблиц, чьё имя совпало с фильтром, поэтому правка СУЩЕСТВУЮЩЕЙ таблицы в заготовку не попадает — так 02.09 потерялись две колонки `payload_locked_documents_rels` и на сутки легло редактирование в админке (разбор — в handoff 04.09). Приёмка теперь ловит это постфактум (снимок по колонкам + дрейф-проверка в `probe-prod.yml`), но заготовку по-прежнему собирает человек. Правильно: снять полную схему эталонной базы, снять схему прода, сравнить, и разницу целиком положить в миграцию. **Триггер возврата:** начало следующей миграции схемы (там же и долг G192 — `.ts`+`.json` снапшот). — added: 2026-09-04, snoozed: 0, last-touch: 2026-09-04

- [ ] **20 черновиков ВКонтакте на проде имеют кириллический slug.** Импорт 03.09 прошёл до того, как `slugFor` научился транслитерировать, поэтому адреса тех записей будут вида `/novosti/%D0%BE%D1%82.../`. Это работает (проверено гейтом: 200 и percent-encoding в sitemap), но выбивается из остальных 128 латинских адресов сайта и мусорит в отчётах Метрики. Лечение — поправить slug в админке при публикации или снести черновики и переимпортировать. **Триггер возврата:** публикация первых постов ВКонтакте владельцем. — added: 2026-09-04, snoozed: 0, last-touch: 2026-09-04

- [ ] **`.ts` + `.json` для миграции `20260823_000001` (правило G192).** В ветке `feat/novosti-collection` лежит только `.sql` — этого достаточно, чтобы применить схему, но недостаточно для **следующей** миграции: без `.json`-снапшота Drizzle считает базу пустой и генерирует правдоподобный `down()` с `DROP TABLE ... CASCADE` по всем таблицам. Пара получается прогоном `payload migrate:create` на пустой базе в CI. **⚠️ Триггер сработал и пропущен:** вторая миграция схемы (`20260904_000001`) создана и применена 04.09 — пары `.ts`/`.json` по-прежнему нет. Осознанное «потом», а не забывчивость: мы вообще не зовём `payload migrate:create` (заготовку снимаем `dump-schema.yml`, применяем сырой `.sql`), поэтому вредный `down()` сгенерировать некому — риск спит, пока никто не запустит `migrate:create`. Цена закрытия — отдельный воркфлоу, который поднимет пустую базу и прогонит `migrate:create`, чтобы получить снапшот. **Новый триггер:** первый запуск `payload migrate:create`/`migrate` в любом виде, либо переход на штатные миграции Payload. — added: 2026-08-23, snoozed: 1, last-touch: 2026-09-04

- [ ] **🔴 ПЕРВЫЙ ШАГ ВОЗВРАТА: обновить Payload 3.75.0 → 3.89.0 и Next 15.5.20 → 15.5.24+ ДО подъёма сайта.** Письмо brain 14.09 (mandate, urgency: high): Payload < 3.79.1 — захват аккаунта **без входа** (pre-auth takeover), Next < 15.5.24 — RCE без аутентификации в image optimization; портал уже видит в журнале прода чужие POST с выдуманными Server Action. Проверено 14.09: у нас `payload` и все `@payloadcms/*` = 3.75.0, `next` в lock = 15.5.20 — мы в классе обеих дыр. Срок 16.09 **сознательно не соблюдён**: сайт остановлен в тот же день, поверхность атаки нулевая (процесса нет, порт не слушается), а прыжок Payload на четырнадцать минорных версий без живого прода для проверки — худший момент. ⚠️ Опасность в порядке действий: поднять сайт и обновиться потом — значит вернуть дыру в сеть на время работы. **Триггер возврата:** решение поднять сайт. Обновление идёт ПЕРЕД `resume`, не после. У brain запрошен чужой разбор перехода 3.75 → 3.89. — added: 2026-09-14, snoozed: 0, last-touch: 2026-09-14

- [ ] **Файл подтверждения Яндекс Вебмастера (mandate D-088, письмо brain 12.09).** Положить в корень `yandex_4f941049c2a8cc91.html` с телом `Verification: 4f941049c2a8cc91`, штатным деплоем; отчёт — `curl` отдаёт 200 с этим телом с прода; «Подтвердить» жмёт владелец. **Сейчас исполнить нельзя:** прод выключен, деплой заперт гейтом парковки, `curl` доказать нечего. Мандат не отклонён — отложен. ⚠️ Владельцу: не нажимать «Подтвердить» в кабинете, пока сайт лежит — проверка прав не пройдёт. **Триггер возврата:** сайт поднят (`park-prod.yml` resume). — added: 2026-09-14, snoozed: 0, last-touch: 2026-09-14

- [ ] **SEO/GEO чек-лист по портфелю (D-088, письмо brain 12.09, `recommend`).** Девять пунктов: ИИ-боты в robots, `llms.txt`, sitemap, JSON-LD, регион и обход по Метрике. Заморожено целиком — ни один пункт не выполним на выключенном сайте, и причина в остановке проекта, а не в несогласии. **Триггер возврата:** сайт поднят. Срез кабинета брать заново — за время паузы он покажет другое. — added: 2026-09-14, snoozed: 0, last-touch: 2026-09-14

- [ ] **Гигиена публичного лога во ВСЕХ воркфлоу разом (письмо brain 11.09, `recommend`).** Наш `probe-prod.yml` разошёлся по кластеру как рецепт, и портал заметил: в публичный лог Actions попадают пути на сервере и имя пользователя SSH. У портала хост, порт, пользователь и путь env берутся из `secrets`/`vars`, в лог идут только имена коллекций и колонок, HTTP-коды и публичные домены. Правка трогает одинаковый SSH-блок в семи воркфлоу — чинить по одному бессмысленно, `park-prod.yml` 14.09 унаследовал тот же блок сознательно. **Триггер возврата:** следующая правка воркфлоу — делать одним заходом по всем файлам. — added: 2026-09-14, snoozed: 0, last-touch: 2026-09-14

- [ ] **Три минутные проверки от соседей (письмо brain 05.09, `recommend`, ack: line).** (1) Имя сессионной cookie: любой хост под общим доменом вправе выставить cookie с `Domain=.<общий домен>`, при двух cookie с одним именем побеждает подброшенная — защищает только префикс `__Host-`; необратимо это становится на действии «привязать внешний аккаунт». (2) `ssh` печатает хост и порт в публичный лог при отказе связи, маскировка секрета этого не ловит; `nginx -t` печатает соседей по боксу. (3) Payload `update`/`find` с `draft: true` работают с версиями, а не с записью. Пункт 2 пересекается с пунктом про гигиену лога выше — брать вместе. **Триггер возврата:** resume сайта (пункты 1 и 3 проверяются только на живом приложении). — added: 2026-09-14, snoozed: 0, last-touch: 2026-09-14
