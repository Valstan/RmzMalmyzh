# PENDING_FOLLOWUPS — отложенное (re-триаж в /start, pool #033)

Формат пункта:

```
- [ ] <что> — added: YYYY-MM-DD, snoozed: 0, last-touch: YYYY-MM-DD
```

- [ ] **Гейт идентичности `CLAUDE.md` ↔ `AGENTS.md`.** Зеркало для Codex регенерируется руками (за сессию 28.07 — трижды), значит однажды разойдётся молча, и заметить будет неоткуда; у brain его собственная копия `AGENTS.md` уже протухала, разойдясь с каноном на целый источник правды. Решение: `scripts/sync-agents-md.mjs` с флагом `--check` (одна точка правды для преобразований: заголовок, «Claude читает» → «Codex читает», `.claude/settings.json` → `.agents/settings.json`), в CI — `--check`, вручную — регенерация. Основание: `suggest` из письма brain `2026-07-28-red-cleared-card-taken-v2-to-owner`. **Триггер возврата:** любая следующая правка `CLAUDE.md` — делать её через скрипт, а не через `sed`. — added: 2026-07-28, snoozed: 0, last-touch: 2026-07-28

- [ ] **Схема Product/Service для каталога «двигатель → услуги»** (`docs/AUDIT-rmz43.md` §4.2, хвост пункта про каталог). Имеет смысл делать **после** решения по v2: если каталог переезжает на корень, разметка ставится сразу на боевые URL, иначе придётся переделывать под `/new/`. **Триггер возврата:** решение руководства по v2. — added: 2026-07-28, snoozed: 0, last-touch: 2026-07-28

- [ ] **Идея #098 от brain — мерить чистую часть без стенда.** Каскадная анкета соискателя (11 шагов, логика пропусков и «— не указано —») и `src/lib/v2/catalog.ts` (6 групп, 33 модели) — чистые функции, их можно проверить числами без поднятия сервера и БД. Актуально тем, что у нас **нет локального стенда вообще** (на PC40 ни Postgres, ни Docker), и каждая проверка стоит прогона CI на 3 минуты. **Триггер возврата:** следующая правка анкеты или каталога. — added: 2026-07-28, snoozed: 0, last-touch: 2026-07-28
