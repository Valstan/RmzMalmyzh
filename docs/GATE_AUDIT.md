# Аудит гейтов #104

Updated: 2026-08-04

## Результат

В контуре пять фактических гейтов; у всех подтверждён реальный красный прогон.

| Гейт | Что ловит | Подтверждение красного |
|---|---|---|
| ESLint (`next/core-web-vitals`, `next/typescript`) | React hooks, Next/React и TS lint-правила | Мутационный прогон 2026-08-04: условный `useState` упал на `react-hooks/rules-of-hooks` |
| TypeScript `tsc --noEmit` | Несовместимые типы и ошибки статической модели | Мутационный прогон 2026-08-04: `string` в `number` упал с TS2322 |
| CI `build` + runtime-content check | Сборка Next/Payload и запрет пререндера контента с пустой БД | Красный run 29613355958; текущая проверка пустая БД → наполнение → `next start` зелёная в PR #34/#35 |
| CI `media-import` | Фазы импорта, файлы/ссылки, восстановление половинчатого состояния, идемпотентность, живая картинка | Красный run 30353695657 на фазе links |
| Deploy + smoke #011 | Доставка/restart, HTTP 200, маркер сайта, `/admin`, XFP-301 | Красный run 29613731229 на доставке; зелёный smoke после PR #35 |

## Найденные дыры

1. ~~**CI не является серверным предусловием merge.**~~ **ЗАКРЫТО 2026-08-17 (не нами).**
   Диагноз был неверен в своей причине: 403 от GitHub API давал не «тариф», а **приватность
   репозитория**. Владелец перевёл все 13 проектных репо в публичные (D-030), и brain включил
   на `main` настоящую серверную защиту с `enforce_admins`, а обязательными проверками сделал
   `build` и `media-import` — красная проверка теперь блокирует влитие на сервере.
   Наш вывод «до изменения тарифа» был обобщением ограничения, верного лишь для части флота
   (идея #160). Процедурный `gh pr checks --watch` остаётся — `--auto` по-прежнему мержит
   при pending checks.
2. **Нет unit/integration-тестов приложения и Semgrep.** ~~gitleaks~~ — **закрыто 2026-08-22**:
   PR-гейт `secret-scan` (gitleaks 8.30.1, `fetch-depth: 0`, `--redact`, `--exit-code 1`),
   мутационная приёмка подсадным ключом проведена. Остаётся: чистая бизнес-логика
   форм/каталога и Semgrep — кандидаты отдельных PR, а не довесок к аудиту.
3. **CI дублируется после merge в `main`.** Это полезная пост-проверка, но не заменяет
   предмержевый гейт и увеличивает время/расход раннера.
